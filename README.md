- Global Error handler middleware 

- JWT -> localStorage Vs. Cookies (HTTPOnly Secure Cookies) and session in Redis

- accessToken Vs. refreshToken

- Payload 

- DB Design (Schema, Model)

- MongoDB Indexing, Aggregation, Sharding, Partitioning 

- OAuth (Social Login)

- S3 Upload (Pre-signed URL)

- Video Play, Upload

- WebSocket

- WebRTC 

- CDN

- Cloudflare 

- Caching

- NextJS Tutorial + Project 

- NextJS Auth (Chai aur Code)

- AI Project 

- SASS Application 

- Generative AI

- Pagination + Throttling + Search + Filter + Debouncing 

- ElasticSearch

- Code Architecture / Separation of Concerns , Optimization 

- Shadcn UI

- TailwindCSS

- Flexbox and Grid layout 

- Figma - UI/UX -> Convert to code (Lovable)

- TypeScript 

- CI/CD Pipeline + GitHub Actions 

- CORS 

- PR Review / Resolve Merge Conflict 

- Swagger / API Docs / Postman Collection (baseURL)

- TanStack Query 

- Redux / Zustand / Context API 

- axiosClient.js and endPoints.js - API URL Management 

- Frontend cannot read the Cookies 

- UUID 

- Complete and Complex Backend App 

- NextJS TypeScript Project 

- refreshFlag (Boolean Toggle) -> trigger useEffect

    You fetch data inside a child component (Card) 

    But the update happens in Parent (Home)

    And we need to tell the Child to re-fetch 


- Firebase 

- Single Responsibility Principle (SRP)

- Frontend Design Patterns 

- Parent-child Communication, Lifting state up, Controlled inputs 

- Build 3-4 modals, multiple parent-child components repeatedly -> see the pattern 

- Nginx 

- Microservices Architecture 

- In Production, we should serve the build static files through NGINX 

    Build the frontend app -> Server using NGINX

    Since, Vite web server only works in Development. For production, you need a web server for frontend. Since React Build is static in production. 

- Your code updates instantly in Docker, since you mounted local folders as Volume. But the Docker image doesn't update automatically.

    Docker compose up --build 

- Production Setup 

    Build backend image -> Build frontend image using Vite build -> Serve Frontend with NGINX -> Use a Reverse Proxy (NGINX) for API calls -> No Volumes, no reload, no dev servers 

    In production, React is static, neeed NGINX to server files and forward API request. 


- React Testing Library, Jest, Mocha 

- Redis, RedisStack, RedisInsigh, ioredis, Persistence 

- Payment Gateway (Razorpay, Stripe)

- Order Management (eCommerce)

- PWA

- Sharding 

- MongoDB Aggregation Pipeline 

- API Placeholder (JSON)

- Stampede Protection (Redis)